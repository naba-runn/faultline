# Faultline — Handoff

This file is a **stub**. A full handoff package (updated
PROJECT_CONTEXT.md, TASKS.md, CHANGELOG.md, folder tree, files
created/modified/remaining, current bugs, pending improvements, latest
+ next git commit, next task, and a ready-to-paste prompt for a new
Claude session) is generated on request — type `HANDOFF` — or
automatically at the end of each milestone.

No handoff has been generated yet. Project is mid–Milestone 1.

If you need to resume work right now without a full handoff: read
PROJECT_CONTEXT.md, then TASKS.md, then the most recent entries in
CHANGELOG.md. That's sufficient to know exactly where things stand.